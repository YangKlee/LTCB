#include <stdio.h>
#include <math.h>
int main()
{
	int a,b,c,d;
	printf("Nhap a, b,c: ");scanf("%d%d%d", &a, &b, &c);
	d = b*b -4*a*c
	if (d >0)
	{
		printf("Phuong trinh co hai nghiem x1 = %f \nx2 = %f", (-b + sqrt(d))/(2*a))
	}
}
